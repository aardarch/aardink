name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CI: true

jobs:
  check:
    name: Lint, format, test, API check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6.2.0
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}
          cache-provider: basic # MIT-licensed

      - name: Make gradlew executable
        run: chmod +x ./${{ env.WORKING_DIR }}/gradlew

      - name: Spotless check
        run: ./gradlew :editor:spotlessCheck :languages:spotlessCheck :sample:spotlessCheck

      - name: Lint
        run: ./gradlew :editor:lint :languages:lint :sample:lintDebug

      - name: Unit tests
        run: ./gradlew :editor:test :languages:test

      - name: Build sample app (debug)
        run: ./gradlew :sample:assembleDebug

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: |
            editor/build/reports/tests/
            languages/build/reports/tests/

      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lint-results
          path: |
            editor/build/reports/lint-results-*.html
            languages/build/reports/lint-results-*.html
            sample/build/reports/lint-results-*.html
